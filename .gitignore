node_modules
build
dist
.sanity
*.png
# ...except the share image and touch icon, which are part of the site.
!public/og.png
!public/apple-touch-icon.png
.env.local
.env.*.local

# Convex codegen
convex/_generated
.vercel
.env*
# ...but the template is documentation, not a secret.
!.env.example
