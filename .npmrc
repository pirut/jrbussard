# react-scripts@5 declares peerOptional typescript "^3.2.1 || ^4", but the
# Convex functions need TypeScript 5 to typecheck. The app itself is plain
# JavaScript and never invokes tsc, so the conflict is spurious — but strict
# peer resolution fails the install on CI without this.
legacy-peer-deps=true
