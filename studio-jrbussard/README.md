# JR Bussard CMS

Sanity Studio for the Notes section on the main site.

Run locally:

```sh
npm install
npm run dev
```

Open `http://localhost:3333` and sign in with Sanity.

The Studio is connected to project `8qiu273i` and dataset `production`.
