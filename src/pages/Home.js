import React from "react";
import Hero from "../components/Hero";

const Home = () => {
    return (
        <main className="page">
            <Hero />
        </main>
    );
};

export default Home;
